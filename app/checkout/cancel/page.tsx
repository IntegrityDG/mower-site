import type { Metadata } from "next";import Link from "next/link";import { getCheckoutSigningSecret } from "@/lib/stripe/config";import { verifyCancelState } from "@/lib/checkout/signed-state";
export const metadata:Metadata={title:"Checkout not completed",robots:{index:false,follow:false}};export const dynamic="force-dynamic";
export default async function Cancel({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;let path="/equipment";if(token){try{path=verifyCancelState(token,getCheckoutSigningSecret())?.returnPath??path}catch{}}return <main className="mx-auto max-w-xl px-6 py-16"><h1 className="text-3xl font-semibold">Payment was not completed</h1><p className="mt-4">No payment status was changed. You can return to browse equipment whenever you are ready.</p><div className="mt-8 flex gap-4"><Link href="/equipment">Browse equipment</Link>{path!=="/equipment"&&<Link href={path}>Return to product</Link>}</div></main>}
