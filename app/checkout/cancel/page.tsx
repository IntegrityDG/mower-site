import type { Metadata } from "next";import Link from "next/link";import { getCheckoutSigningSecret } from "@/lib/stripe/config";import { verifyCancelState } from "@/lib/checkout/signed-state";
export const metadata:Metadata={title:"Checkout not completed",robots:{index:false,follow:false}};export const dynamic="force-dynamic";
export default async function Cancel({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;let path="/equipment";let reference:string|null=null;if(token){try{const state=verifyCancelState(token,getCheckoutSigningSecret());path=state?.returnPath??path;reference=state?.publicReference??null}catch{}}return <main className="mx-auto max-w-xl px-6 py-16"><h1 className="text-3xl font-semibold">Payment was not completed</h1><p className="mt-4">No payment status was changed. You can return to browse equipment whenever you are ready.</p>{reference&&<section className="mt-6 rounded-xl border border-emerald-200 bg-emerald-50 p-4"><h2 className="font-black text-emerald-950">IDS order reference</h2><p className="mt-2 break-all font-mono text-lg font-black text-emerald-950">{reference}</p><p className="mt-2 text-sm text-emerald-900">If IDS asked you to authorize a private Demo Party benefit, copy this reference into your secure appointment portal.</p></section>}<div className="mt-8 flex gap-4"><Link href="/equipment">Browse equipment</Link>{path!=="/equipment"&&<Link href={path}>Return to product</Link>}</div></main>}
