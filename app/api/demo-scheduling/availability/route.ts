import { getAvailableSlots, getPublicDemoAreaPlanning } from "@/lib/demo-scheduling/server";
import { APPOINTMENT_TYPE_CONFIG } from "@/lib/scheduling/config";
import { validateDateRange } from "@/lib/demo-scheduling/validation";
export async function GET(request:Request){const p=new URL(request.url).searchParams,range=validateDateRange(p.get("start"),p.get("end"));if(!range)return Response.json({error:"Choose a valid date range of 42 days or fewer."},{status:400});try{const[slots,areaPlanning]=await Promise.all([getAvailableSlots(range.start,range.end),getPublicDemoAreaPlanning(range.start,range.end)]);return Response.json({timezone:"America/Chicago",appointmentType:"demo",durationMinutes:APPOINTMENT_TYPE_CONFIG.demo.durationMinutes,slots,areaPlanning});}catch{return Response.json({error:"Demo availability is temporarily unavailable."},{status:503});}}
