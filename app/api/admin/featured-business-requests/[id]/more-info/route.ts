import { isReviewAdmin } from "@/lib/reviews/admin-auth";import { requestMoreInfo } from "@/lib/featured-businesses/request-server";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!(await isReviewAdmin()))return Response.json({error:"Unauthorized"},{status:401});const body=await request.json().catch(()=>({}));if(typeof body.message!=="string"||!body.message.trim())return Response.json({error:"Enter the information you need from the applicant."},{status:400});try{await requestMoreInfo((await params).id,body.message);return Response.json({success:true});}catch{return Response.json({error:"The message could not be sent. The request status was not changed."},{status:502});}}
